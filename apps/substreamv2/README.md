# DripSwap Uniswap V2 Events Substream

This package outputs only raw Uniswap V2 events (PairCreated/Transfer/Mint/Burn/Swap/Sync)
as protobuf, leaving all calculations and database writes to the backend.
