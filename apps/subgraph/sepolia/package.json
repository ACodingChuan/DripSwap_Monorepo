{
  "name": "dripswap-sepolia",
  "packageManager": "pnpm@10.16.1",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node ${GRAPH_NODE:-https://api.studio.thegraph.com/deploy/} ${GRAPH_NAME:-dripswap-sepolia}",
    "create-local": "graph create --node ${GRAPH_NODE:-http://localhost:8020/} ${GRAPH_NAME:-dripswap-sepolia}",
    "remove-local": "graph remove --node ${GRAPH_NODE:-http://localhost:8020/} ${GRAPH_NAME:-dripswap-sepolia}",
    "deploy-local": "graph deploy --node ${GRAPH_NODE:-http://localhost:8020/} --ipfs ${GRAPH_IPFS:-http://localhost:5001} ${GRAPH_NAME:-dripswap-sepolia}",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.35.1"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
